draft a document docs/dev/spec/usecases/proposed-branching-strategy.md that describes a git branching strategy where there is a development branch that keeps detailed commits and a release branch where for each release several detailed commits are squashed into a single commit on the release branch. A git tag is kept on the detailed development branch corresponding to the commit on the release branch.   If this creates a history problem for later releases because the history on the detailed branch is not on the release branch, propose an alternative approach, or explain how to work around the history problem.  Development wil be done on feature branches that will be short lived while the feature is being worked on, and deleted when the feature with all of its commits are merged back to the development detail branch.

result is now in the {dev-scripts}/docs/dev/spec/usecases/proposed-squash-n-detail-branch-strategy.md