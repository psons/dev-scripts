# save named filesets

create a python utility named fileset in the bin directory that has athe ability to save and recall a named set of files as they would be used on a shell command line as arguments to the tar command.

